# 2.0.5

- Fixed a crash after saving plugin settings and then handling a Twitch chat command.
- Fixed multi-command matching so all configured commands are recognized reliably.
- Hardened command parsing for Unicode whitespace and common separators.
- Kept the release package limited to `kamyshtracker.dll` and KamyshTracker data files.
