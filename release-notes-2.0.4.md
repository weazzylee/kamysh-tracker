# 2.0.4

- Fixed OBS load failure caused by a missing `Qt6WebSockets.dll` dependency.
- Replaced the EventSub WebSocket runtime dependency on Qt WebSockets with WinHTTP WebSocket.
- The release package still contains only `kamyshtracker.dll` and KamyshTracker data files.
- OBS Studio baseline remains 30.0.0+.
